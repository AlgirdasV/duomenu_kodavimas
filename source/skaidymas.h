#ifndef SKAIDYMAS_H
#define SKAIDYMAS_H
#include <string>
#include "kunas.h"

	void skaidyti_faila(std::string, int);
	Vektorius char_to_binary(char);
	unsigned char binary_to_char(Vektorius );
#endif
