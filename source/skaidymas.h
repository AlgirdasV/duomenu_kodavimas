#ifndef SKAIDYMAS_H
#define SKAIDYMAS_H
#include <string>
#include "kunas.h"

    void skaidyti_faila(std::string, int,int);
    Vektorius char_to_binary(char);
    unsigned char binary_to_char(Vektorius );
    Vektorius int_to_binary(unsigned );
    unsigned binary_to_int(Vektorius);
#endif
